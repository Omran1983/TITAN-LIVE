# Block Manifest — HR Compliance
block = "hr_compliance"
block_version = "1.0.0"
schema_version = "1.0"

# Entrypoint script (must exist)
entrypoint = "validator.py"

# Sample input used for verification (must exist)
sample_input = "sample/employees_sample.csv"

# Where JSON/PDF reports are written
report_dir = "reports"
